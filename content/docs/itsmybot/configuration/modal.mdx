---
title: Modal
description: Learn how to configure modals in ItsMyBot.
---

Modals let you collect structured input from the user.

ItsMyBot modals support [placeholders](/docs/itsmybot/configuration/placeholders/) across titles, labels, descriptions, and input values, so the modal can adapt to the current context before it is shown.

If you want a faster starting point, open the [Modal Builder](/docs/itsmybot/builder?tab=modal). It generates the same YAML structure documented here.

## Modal Configuration

At the top level, a modal contains:

- a `title`
- a `custom-id`
- a `components` array

Top-level modal components are limited to:

- `text-display`
- `label`

<TypeTable
  type={{
    title: {
      description: 'Text shown in the modal header.',
      type: 'string',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to resolve the submitted modal. Use a `script_` prefix if you want to catch the submit in scripts.',
      type: 'string',
      required: true,
    },
    components: {
      description: 'List of top-level modal components. Only `text-display` and `label` are valid here.',
      type: 'Component[]',
      required: true,
    },
  }}
/>

```yaml title="Example"
title: "Feedback"
custom-id: "script_feedback_modal"
components:
  - type: text-display
    content: "Tell us what happened and attach files if needed."

  - type: label
    label: "Message"
    description: "Explain the issue clearly."
    component:
      type: text-input
      style: "paragraph"
      custom-id: "message"
      required: true
      min-length: 10

  - type: label
    label: "Category"
    description: "Choose one category."
    component:
      type: radio-group
      custom-id: "category"
      required: true
      options:
        - label: "Bug"
          value: "bug"
        - label: "Suggestion"
          value: "suggestion"

  - type: label
    label: "Attachments"
    description: "Upload screenshots or files."
    component:
      type: file-upload
      custom-id: "files"
      max-values: 3
```

## Component Conditions

Modal components support the same condition system used elsewhere in ItsMyBot. This lets you show or hide parts of the modal depending on context.

You can find the available condition list in [Scripting Conditions](/docs/itsmybot/scripting/conditions/).

<TypeTable
  type={{
    conditions: {
      description: 'List of conditions that must pass before the component is included in the modal.',
      type: 'Condition[]',
      required: false,
      typeDescriptionLink: '/docs/itsmybot/scripting/conditions',
    },
  }}
/>

## Top-Level Components

### `text-display`

Use `text-display` when you want to explain the modal before the user starts filling fields.

<TypeTable
  type={{
    type: {
      type: 'text-display',
      description: 'Component type.',
      required: true,
    },
    content: {
      description: 'Displayed text. Can be a string or a string array for randomized content.',
      type: 'string | string[]',
      required: true,
    },
  }}
/>

```yaml title="Example"
components:
  - type: text-display
    content: "Please complete every required field."
```

### `label`

`label` is the field wrapper used inside modals.

It renders:

- a visible label
- an optional description
- exactly one modal input component

Supported input components inside `label.component`:

- `text-input`
- `select-menu`
- `file-upload`
- `checkbox`
- `checkbox-group`
- `radio-group`

<TypeTable
  type={{
    type: {
      type: 'label',
      description: 'Component type.',
      required: true,
    },
    label: {
      description: 'Field title shown above the input.',
      type: 'string',
      required: true,
    },
    description: {
      description: 'Optional helper text shown under the label.',
      type: 'string',
      required: false,
    },
    component: {
      description: 'The actual input rendered inside the field.',
      type: 'Component',
      required: true,
    },
  }}
/>

```yaml title="Example"
components:
  - type: label
    label: "Username"
    description: "Enter the account name you want to use."
    component:
      type: text-input
      custom-id: "username"
      style: "short"
      required: true
```

## Input Components

### `text-input`

Use `text-input` for free-form user text.

<TypeTable
  type={{
    type: {
      type: 'text-input',
      description: 'Input type.',
      required: true,
    },
    style: {
      description: 'Input layout. Use `short` for one line or `paragraph` for larger text.',
      type: 'string',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to read the submitted value.',
      type: 'string',
      required: true,
    },
    placeholder: {
      description: 'Placeholder shown while the field is empty.',
      type: 'string | string[]',
      required: false,
    },
    'min-length': {
      description: 'Minimum number of characters required.',
      type: 'integer',
      required: false,
    },
    'max-length': {
      description: 'Maximum number of characters allowed.',
      type: 'integer',
      required: false,
    },
    required: {
      description: 'Whether the user must fill the input before submitting.',
      type: 'boolean',
      required: false,
    },
    value: {
      description: 'Prefilled value shown when the modal opens.',
      type: 'string | string[]',
      required: false,
    },
  }}
/>

```yaml title="Example"
component:
  type: text-input
  style: "paragraph"
  custom-id: "details"
  placeholder: "Explain the issue"
  min-length: 10
  max-length: 1000
  required: true
```

### `select-menu`

Use `select-menu` when the user should choose from a predefined list.

<TypeTable
  type={{
    type: {
      type: 'select-menu',
      description: 'Input type.',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to read the selected values.',
      type: 'string | string[]',
      required: true,
    },
    options: {
      description: 'Available options shown in the menu.',
      type: 'SelectOption[]',
      required: true,
    },
    placeholder: {
      description: 'Placeholder text shown before the user selects anything.',
      type: 'string | string[]',
      required: false,
    },
    'min-values': {
      description: 'Minimum number of selected entries.',
      type: 'integer',
      required: false,
    },
    'max-values': {
      description: 'Maximum number of selected entries.',
      type: 'integer',
      required: false,
    },
  }}
/>

#### `select-menu` options

<TypeTable
  type={{
    label: {
      description: 'Visible option label.',
      type: 'string | string[]',
      required: true,
    },
    value: {
      description: 'Stored option value used in the submission.',
      type: 'string | string[]',
      required: true,
    },
  }}
/>

```yaml title="Example"
component:
  type: select-menu
  custom-id: "fruits"
  placeholder: "Select your favorite fruits"
  min-values: 1
  max-values: 2
  options:
    - label: "Apple"
      value: "apple"
    - label: "Orange"
      value: "orange"
```

### `file-upload`

Use `file-upload` when the user should attach screenshots, logs, or other files.

<TypeTable
  type={{
    type: {
      type: 'file-upload',
      description: 'Input type.',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to read uploaded attachments after submit.',
      type: 'string | string[]',
      required: true,
    },
    required: {
      description: 'Whether at least one file must be uploaded.',
      type: 'boolean',
      required: false,
    },
    'min-values': {
      description: 'Minimum number of files the user must upload.',
      type: 'integer',
      required: false,
    },
    'max-values': {
      description: 'Maximum number of files the user can upload.',
      type: 'integer',
      required: false,
    },
  }}
/>

```yaml title="Example"
component:
  type: file-upload
  custom-id: "attachments"
  required: false
  max-values: 3
```

### `checkbox`

Use `checkbox` for a single boolean choice.

<TypeTable
  type={{
    type: {
      type: 'checkbox',
      description: 'Input type.',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to read the submitted boolean value.',
      type: 'string | string[]',
      required: true,
    },
    default: {
      description: 'Whether the checkbox is preselected when the modal opens.',
      type: 'boolean',
      required: false,
    },
  }}
/>

```yaml title="Example"
component:
  type: checkbox
  custom-id: "agree_rules"
  default: true
```

### `checkbox-group`

Use `checkbox-group` when the user can select multiple options from one field.

<TypeTable
  type={{
    type: {
      type: 'checkbox-group',
      description: 'Input type.',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to read the submitted values.',
      type: 'string | string[]',
      required: true,
    },
    required: {
      description: 'Whether the user must choose at least one option.',
      type: 'boolean',
      required: false,
    },
    'min-values': {
      description: 'Minimum number of selected options.',
      type: 'integer',
      required: false,
    },
    'max-values': {
      description: 'Maximum number of selected options.',
      type: 'integer',
      required: false,
    },
    options: {
      description: 'Available checkbox options.',
      type: 'ChoiceOption[]',
      required: true,
    },
  }}
/>

#### `checkbox-group` options

<TypeTable
  type={{
    label: {
      description: 'Visible option label.',
      type: 'string | string[]',
      required: true,
    },
    value: {
      description: 'Stored option value used in the submission.',
      type: 'string | string[]',
      required: true,
    },
    description: {
      description: 'Optional helper text shown under the option label.',
      type: 'string | string[]',
      required: false,
    },
    default: {
      description: 'Whether the option is preselected when the modal opens.',
      type: 'boolean',
      required: false,
    },
  }}
/>

```yaml title="Example"
component:
  type: checkbox-group
  custom-id: "platforms"
  required: true
  min-values: 1
  max-values: 2
  options:
    - label: "Java"
      value: "java"
      description: "Java Edition players"
    - label: "Bedrock"
      value: "bedrock"
      description: "Bedrock Edition players"
      default: true
```

### `radio-group`

Use `radio-group` when the user must choose one option from a list.

<TypeTable
  type={{
    type: {
      type: 'radio-group',
      description: 'Input type.',
      required: true,
    },
    'custom-id': {
      description: 'Identifier used to read the submitted value.',
      type: 'string | string[]',
      required: true,
    },
    required: {
      description: 'Whether a choice is required before submit.',
      type: 'boolean',
      required: false,
    },
    options: {
      description: 'Available radio options.',
      type: 'ChoiceOption[]',
      required: true,
    },
  }}
/>

#### `radio-group` options

<TypeTable
  type={{
    label: {
      description: 'Visible option label.',
      type: 'string | string[]',
      required: true,
    },
    value: {
      description: 'Stored option value used in the submission.',
      type: 'string | string[]',
      required: true,
    },
    description: {
      description: 'Optional helper text shown under the option label.',
      type: 'string | string[]',
      required: false,
    },
    default: {
      description: 'Whether the option is preselected when the modal opens.',
      type: 'boolean',
      required: false,
    },
  }}
/>

```yaml title="Example"
component:
  type: radio-group
  custom-id: "priority"
  required: true
  options:
    - label: "Low"
      value: "low"
      description: "General issue with no immediate urgency"
    - label: "High"
      value: "high"
      description: "Needs quick attention"
      default: true
```

## Script Interactions

To detect a modal submission with a script, the modal `custom-id` must start with `script_`.

See [Script interaction custom-id format](/docs/itsmybot/scripting/triggers#interaction-custom-id) for argument parsing, and [modalSubmit](/docs/itsmybot/scripting/triggers#modalsubmit) for the submitted variables.
